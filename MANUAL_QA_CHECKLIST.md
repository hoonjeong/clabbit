# 🎯 클래빗 수동 QA 체크리스트

이 문서는 서버를 실행한 후 직접 브라우저에서 수행할 QA 항목들입니다.

---

## 🚀 시작하기

### 1단계: 서버 실행
```bash
npm start
```

### 2단계: 브라우저 개발자 도구 열기
- **Chrome**: F12 또는 Ctrl+Shift+I
- **Console 탭** 항상 열어두기 (빨간색 에러 확인용)
- **Network 탭** 준비 (API 요청 실패 확인용)

### 3단계: 로그인
```
URL: http://localhost:3000
1. 회원가입 또는 로그인
2. 학원 등록/선택
3. 대시보드 진입
```

---

## ✅ 빠른 QA (30분)

### 🎯 필수 점검 항목

#### 1. 사이드바 메뉴 전체 테스트 (10분)
```
목표: 모든 메뉴가 작동하는지 확인

□ 대시보드 클릭 → 페이지 로드 확인
□ 학생 관리 → 학생 목록 클릭 → 페이지 로드
  □ 학생 등록 클릭
  □ 엑셀 일괄등록 클릭
  □ 퇴원생 관리 클릭
□ 수업 관리 → 수업 목록 클릭
  □ 수업 추가 클릭
  □ 강사 관리 클릭
□ 출결 관리 → 모든 하위 메뉴 클릭
  □ 출결 대시보드
  □ 출결 현황
  □ 횟수제 관리
  □ 보강 수업
  □ 출결 통계
□ 청구/수납 → 모든 하위 메뉴 클릭
  □ 청구/수납 대시보드
  □ 청구 관리
  □ 수납 관리
  □ 미납자 관리
  □ 청구 항목 설정
  □ 청구/수납 통계

각 페이지에서 확인:
✓ 콘솔에 빨간색 에러 없음
✓ 페이지 제목이 표시됨
✓ 레이아웃이 깨지지 않음
```

#### 2. 학생 등록 → 목록 확인 (5분)
```
목표: 핵심 CRUD 기능 작동 확인

□ 학생 관리 → 학생 등록
□ 필수 정보 입력:
  - 이름: 홍길동
  - 전화번호: 010-1234-5678
  - 학년: 초등 3
  - 보호자: 홍부모
  - 보호자 전화: 010-9876-5432
□ 저장 버튼 클릭
□ 성공 메시지 확인
□ 학생 목록으로 이동
□ 방금 등록한 학생이 목록에 보이는지 확인
□ 학생 이름 클릭 → 상세 페이지
□ 정보가 올바른지 확인
```

#### 3. 검색 기능 테스트 (3분)
```
목표: 검색이 작동하는지 확인

□ 학생 목록 페이지
□ 검색창에 "홍길동" 입력
□ 검색 결과가 필터링되는지 확인
□ 검색어 지우기 → 전체 목록 다시 표시
```

#### 4. 반응형 테스트 (5분)
```
목표: 모바일에서도 작동하는지 확인

□ F12 → Device Toolbar (Ctrl+Shift+M)
□ iPhone SE 선택
□ 대시보드 확인
  □ 레이아웃 정상
  □ 사이드바가 햄버거 메뉴로 변경
  □ 모든 버튼 클릭 가능
□ 학생 목록 확인
  □ 가로 스크롤 없음
  □ 표가 적절히 표시
```

#### 5. 콘솔 에러 전수 조사 (7분)
```
목표: 숨겨진 JavaScript 에러 찾기

모든 페이지를 한 번씩 열면서 콘솔 확인:
□ 대시보드
□ 학생 목록
□ 학생 등록
□ 수업 목록
□ 출결 대시보드
□ 청구/수납 대시보드

발견된 에러는 즉시 기록:
- 페이지명:
- 에러 메시지:
- 재현 방법:
```

---

## 🔍 상세 QA (2시간)

### 📋 학생 관리 모듈 (30분)

#### 학생 목록 페이지
```
□ 페이지 로드 시간 3초 이내
□ 학생 데이터 테이블 표시
□ 검색창 작동
  □ 한글 입력 → 검색
  □ 영문 입력 → 검색
  □ 특수문자 입력 → 에러 없음
□ 필터 기능 (있다면)
  □ 학년 필터
  □ 상태 필터 (활동/비활동)
□ 정렬 기능
  □ 이름 컬럼 클릭 → 정렬
  □ 등록일 컬럼 클릭 → 정렬
□ 페이징 (데이터 많을 경우)
  □ 다음 페이지
  □ 이전 페이지
  □ 페이지 직접 선택
```

#### 학생 등록 페이지
```
□ 페이지 로드 확인
□ 모든 입력 필드 표시 확인

각 필드 개별 테스트:
□ 이름 필드
  □ 한글 입력 가능
  □ 영문 입력 가능
  □ 빈 칸 → 에러 메시지
  □ 50자 이상 → 제한 또는 에러
□ 전화번호 필드
  □ 숫자만 입력
  □ 자동 하이픈 삽입 (010-1234-5678)
  □ 11자리 제한
□ 생년월일 필드
  □ 날짜 선택기 작동
  □ 미래 날짜 → 에러
  □ 직접 타이핑 가능
□ 학년 선택
  □ 드롭다운 열림
  □ 선택 가능
□ 주소 필드
  □ 긴 주소 입력 가능
□ 보호자 정보
  □ 이름 입력
  □ 전화번호 입력
□ 이메일 (있다면)
  □ 올바른 형식 체크
  □ abc@example.com 형식

유효성 검사 테스트:
□ 이름 빈 칸 → "이름을 입력하세요" 에러
□ 전화번호 빈 칸 → 에러
□ 잘못된 이메일 형식 → 에러

폼 제출 테스트:
□ 모든 필드 입력
□ 저장 버튼 클릭
□ 로딩 인디케이터 표시
□ 버튼 비활성화 (중복 클릭 방지)
□ 성공 메시지 표시
□ 목록 페이지로 리다이렉트
□ 새 학생이 목록에 보임

취소 버튼:
□ 클릭 → 확인 다이얼로그
□ 확인 → 데이터 저장 안 됨
```

#### 학생 상세/수정 페이지
```
□ 페이지 로드
□ 학생 정보 올바르게 표시
□ 사진 표시 (있다면)

정보 확인:
□ 이름 맞음
□ 전화번호 맞음
□ 생년월일 맞음
□ 등록일 표시
□ 상태 표시

수정 기능:
□ 이름 변경 → 저장 → 반영 확인
□ 전화번호 변경 → 저장 → 반영
□ 여러 필드 동시 변경 → 모두 반영

관련 정보 (탭이 있다면):
□ 출석 이력 탭 → 데이터 표시
□ 수납 이력 탭 → 데이터 표시

액션 버튼:
□ 삭제 버튼 → 확인 다이얼로그
□ 삭제 확인 → 학생 삭제
```

---

### 📅 출결 관리 모듈 (30분)

#### 출석 체크 페이지
```
□ 페이지 로드
□ 오늘 날짜 기본 선택
□ 학생 목록 표시

날짜 선택:
□ 날짜 선택기 열림
□ 다른 날짜 선택 → 데이터 로드
□ 과거 날짜 선택 → 기존 데이터 표시

출석 체크:
□ 첫 번째 학생 "출석" 클릭 → 상태 변경
□ "지각" 클릭 → 시간 입력 필드 나타남
□ 시간 입력 → 저장
□ "결석" 클릭 → 사유 입력 필드
□ 사유 입력 → 저장

일괄 기능 (있다면):
□ "전체 출석" 버튼 → 모든 학생 출석
□ "전체 선택" 체크박스 → 선택

저장:
□ 여러 학생 체크 후 저장
□ 로딩 표시
□ 성공 메시지
□ 새로고침 후 데이터 유지
```

#### 출결 현황 페이지
```
□ 페이지 로드
□ 출결 데이터 표시
□ 통계 표시

날짜 범위 선택:
□ 시작일 선택
□ 종료일 선택
□ 검색 버튼 → 해당 기간 데이터 표시

빠른 날짜 선택 (있다면):
□ "오늘" 버튼
□ "이번 주" 버튼
□ "이번 달" 버튼

테이블:
□ 날짜별 표시
□ 학생별 표시
□ 출석/지각/결석 색상 구분
□ 정렬 기능

통계:
□ 총 출석률 정확
□ 출석/지각/결석 수 정확
□ 계산 확인 (수동)

엑셀 다운로드 (있다면):
□ 버튼 클릭 → 파일 다운로드
□ 파일 열기 → 데이터 확인
□ 한글 안 깨짐
```

---

### 💰 청구/수납 모듈 (30분)

#### 수납 등록 페이지
```
□ 페이지 로드
□ 폼 표시

학생 선택:
□ 드롭다운 클릭
□ 학생 목록 표시
□ 학생 선택 → 미수금 자동 표시

수납 정보 입력:
□ 금액 입력
  □ 숫자만 입력
  □ 천 단위 콤마 자동 삽입
  □ 음수 입력 → 에러
  □ 0원 입력 → 에러
□ 수납 방법 선택
  □ 현금
  □ 카드 → 카드사, 할부 입력
  □ 계좌이체 → 은행명 입력
□ 수납 날짜
  □ 기본값 오늘
  □ 다른 날짜 선택 가능
□ 메모 입력

저장:
□ 모든 필드 입력 → 저장
□ 로딩 표시
□ 성공 메시지
□ 목록으로 이동
□ 미수금 자동 차감 확인

유효성 검사:
□ 학생 미선택 → 에러
□ 금액 미입력 → 에러
□ 수납 방법 미선택 → 에러

초과 납부:
□ 미수금보다 많은 금액 → 경고
```

#### 수납 내역 페이지
```
□ 페이지 로드
□ 수납 내역 테이블 표시
□ 최신 순 정렬

필터:
□ 날짜 범위 선택
□ 학생 이름 검색
□ 수납 방법 필터

테이블:
□ 날짜 정렬
□ 금액 정렬
□ 상세보기 클릭 → 정보 표시

수정/삭제:
□ 수정 버튼 → 수정 페이지
□ 삭제 버튼 → 확인 다이얼로그
□ 삭제 → 미수금 증가 확인

통계 (있다면):
□ 총 수납액 정확
□ 현금/카드/계좌 별 금액 정확

영수증 출력 (있다면):
□ 출력 버튼 → 인쇄 미리보기
□ 모든 정보 포함
```

#### 미수금 관리 페이지
```
□ 페이지 로드
□ 미수금 있는 학생 목록

테이블:
□ 이름, 미수금액, 마지막 수납일
□ 미수금 많은 순 정렬
□ 금액 정확성 확인

정렬:
□ 미수금액 컬럼 클릭
□ 마지막 수납일 컬럼 클릭

독촉 기능 (있다면):
□ 학생 선택 → 독촉 문자 발송
```

---

### 📊 통계 및 보고서 (20분)

#### 종합 통계 페이지
```
□ 페이지 로드
□ 차트 렌더링

위젯 확인:
□ 학생 수 추이 차트
□ 매출 추이 차트
□ 출석률 차트
□ 미수금 현황

기간 선택:
□ "이번 주" 버튼
□ "이번 달" 버튼
□ "올해" 버튼
□ 커스텀 날짜 범위

차트 인터랙션:
□ 호버 → 툴팁 표시
□ 범례 클릭 → show/hide

데이터 정확성:
□ 학생 수 DB와 일치
□ 매출 합계 수납 내역과 일치
□ 출석률 계산 확인 (수동)
```

---

## 🌐 반응형 테스트 (10분)

```
모든 해상도에서 주요 페이지 테스트:

□ 모바일 (375px)
  □ 대시보드
  □ 학생 목록
  □ 학생 등록
  □ 가로 스크롤 없음
  □ 햄버거 메뉴 작동
  □ 버튼 클릭 가능 (44px 이상)

□ 태블릿 (768px)
  □ 레이아웃 적절
  □ 카드 배치 적절

□ 데스크톱 (1920px)
  □ 모든 요소 보임
  □ 레이아웃 적절
```

---

## 🎯 발견된 문제 기록 양식

```markdown
### 문제 #번호: 제목

**우선순위**: P0/P1/P2/P3

**페이지**: 페이지명

**재현 단계**:
1.
2.
3.

**기대 동작**:
~해야 함

**실제 동작**:
~됨

**콘솔 에러**:
```
(에러 메시지 복사)
```

**스크린샷**:
(필요시)

**해결 방법**:
(알고 있다면)
```

---

## ✅ QA 완료 체크리스트

```
□ 빠른 QA (30분) 완료
□ 상세 QA (2시간) 완료
□ 반응형 테스트 완료
□ 발견된 문제 모두 기록
□ P0, P1 문제 즉시 보고
□ 수정 후 재테스트
```

---

## 📝 참고사항

### 우선순위 정의
- **P0 (치명적)**: 페이지 크래시, 데이터 손실, 보안 이슈
- **P1 (주요)**: 핵심 기능 작동 불가, 심각한 UI 깨짐
- **P2 (일반)**: 부가 기능 오류, 사소한 UI 문제
- **P3 (경미)**: UX 개선, 디자인 미세 조정

### 테스트 팁
1. 천천히, 꼼꼼히
2. 실제 사용자처럼 테스트
3. 엣지 케이스 고려 (빈 입력, 특수문자, 매우 긴 텍스트)
4. 콘솔 항상 확인
5. 네트워크 탭 확인
6. 즉시 기록 (나중에 잊어버림)
7. 재현 가능성 확인

---

*이 체크리스트를 사용하여 체계적인 QA를 진행하세요!*
